const API=import.meta.env.VITE_ANALYTICS_URL||'/analytics/api/v1';export async function apiGet(path,{signal}={}){const token=localStorage.getItem('shahajjo_token');const response=await fetch(`${API}${path}`,{signal,headers:{Accept:'application/json',...(token?{Authorization:`Bearer ${token}`}:{})}});if(!response.ok)throw new Error(`Analytics request failed (${response.status})`);return response.json()}
