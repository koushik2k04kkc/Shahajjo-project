import{apiGet}from'../../../lib/apiClient';const API=import.meta.env.VITE_API_URL||'/api/v1';async function mutate(path,method,body){const r=await fetch(`${API}${path}`,{method,headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});if(!r.ok)throw new Error(`Case request failed (${r.status})`);return r.json()}export const getCases=signal=>apiGet('/cases',{signal});export const getCase=(id,signal)=>apiGet(`/cases/${id}`,{signal});export const assignCase=(id,assigneeId)=>mutate(`/cases/${id}/assign`,'PATCH',{assigneeId});export const updateCase=(id,updates)=>mutate(`/cases/${id}`,'PATCH',updates)
