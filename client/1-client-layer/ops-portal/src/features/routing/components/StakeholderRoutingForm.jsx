import{useState}from'react';import{routeCase}from'../api/routingApi';export default function StakeholderRoutingForm({alertId,onRouted}){const[form,setForm]=useState({team:'Area operations',assigneeId:'',note:''});const[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);try{const result=await routeCase({alertId,...form});onRouted?.(result)}finally{setBusy(false)}}return <form onSubmit={submit} className="space-y-4"><label className="block text-xs font-bold">Team<select value={form.team} onChange={e=>setForm({...form,team:e.target.value})} className="mt-2 w-full rounded-xl border p-3"><option>Area operations</option><option>Provider operations</option><option>Risk review</option></select></label><label className="block text-xs font-bold">Assignee<input value={form.assigneeId} onChange={e=>setForm({...form,assigneeId:e.target.value})} className="mt-2 w-full rounded-xl border p-3" placeholder="Staff ID"/></label><label className="block text-xs font-bold">Note<textarea value={form.note} onChange={e=>setForm({...form,note:e.target.value})} className="mt-2 w-full rounded-xl border p-3" rows="3"/></label><button disabled={busy} className="rounded-xl bg-brand px-4 py-2 text-sm font-bold text-white disabled:opacity-60">Route case</button></form>}
