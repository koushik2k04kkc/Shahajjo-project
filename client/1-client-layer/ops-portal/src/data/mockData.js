// Localization audit changelog: removed seeded example content so the portal renders only empty defaults until real data is available.
export const mockOverview = {
  agent: { id: '', name: '', nameBn: '', location: '', locationBn: '', lastSync: '' },
  status: '',
  balances: [],
}

export const mockLiquidity = { confidence: 0, shortageWindow: { start: 0, end: 0, label: '', labelBn: '' }, points: [] }

export const mockAlerts = []

export const mockCases = []

export const mockMetrics = []
