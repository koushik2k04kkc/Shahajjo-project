import{io}from'socket.io-client';let socket;export function getSocket(){if(!socket)socket=io(import.meta.env.VITE_SOCKET_URL||window.location.origin,{autoConnect:false,transports:['websocket'],auth:{token:localStorage.getItem('shahajjo_token')}});return socket}export function subscribeToAlerts(handler){const client=getSocket();client.on('alert:area',handler);client.connect();return()=>{client.off('alert:area',handler);client.disconnect()}}
