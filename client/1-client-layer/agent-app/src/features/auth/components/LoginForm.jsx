import { useState } from 'react'
import { useTranslation } from 'react-i18next'
export default function LoginForm({onLogin}){const{t}=useTranslation();const[form,setForm]=useState({mobile:'',pin:''});const[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);await onLogin(form);setBusy(false)}return <main className="grid min-h-screen place-items-center bg-canvas p-5"><form onSubmit={submit} className="w-full max-w-sm rounded-3xl border border-slate-200 bg-white p-7 shadow-card"><div className="mb-7 flex items-center gap-3"><div className="grid size-11 place-items-center rounded-2xl bg-brand text-xl font-bold text-white">স</div><div><h1 className="text-xl font-bold">{t('brand')}</h1><p className="text-xs text-slate-500">{t('loginHint')}</p></div></div><label className="text-xs font-semibold text-slate-600">{t('mobile')}<input value={form.mobile} onChange={e=>setForm({...form,mobile:e.target.value})} inputMode="tel" className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-brand" placeholder="01XXXXXXXXX"/></label><label className="mt-4 block text-xs font-semibold text-slate-600">{t('pin')}<input value={form.pin} onChange={e=>setForm({...form,pin:e.target.value})} type="password" inputMode="numeric" className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-brand" placeholder="••••"/></label><button disabled={busy} className="mt-6 w-full rounded-xl bg-brand py-3 text-sm font-bold text-white disabled:opacity-60">{t('login')}</button><button type="button" onClick={()=>onLogin({})} className="mt-3 w-full py-2 text-xs font-semibold text-brand">{t('demo')}</button></form></main>}
