import { useTranslation } from 'react-i18next'
const money=n=>new Intl.NumberFormat('bn-BD',{maximumFractionDigits:0}).format(n)
export default function BalanceCard({item}){const{t}=useTranslation();const stale=item.amount==null;return <article className="rounded-2xl border border-slate-200 bg-white p-4 shadow-card"><div className="flex items-center justify-between"><span className="flex items-center gap-2 text-sm font-semibold"><i className="size-2.5 rounded-full" style={{background:item.color}}/>{item.name}</span><span className="text-[10px] text-slate-400">{t('updated')} {item.updated}</span></div>{stale?<><p className="mt-5 font-semibold text-slate-400">—</p><p className="mt-2 text-xs text-slate-500">● {t('delayed')} {item.delayed}m</p></>:<><p className="mt-5 text-2xl font-bold">৳{money(item.amount)}</p><p className={`mt-2 text-xs font-bold ${item.trend.startsWith('+')?'text-emerald-600':'text-orange-600'}`}>{item.trend}</p></>}</article>}
